/**
 * Settings Feature
 *
 * Feature module for settings-related screens and utilities.
 * The actual screens are in app/(main)/settings/*.
 */

// Re-export types that might be useful
export type { UserRole } from '../../constants/userRoles';
export { USER_ROLES } from '../../constants/userRoles';
